# vscode-typescript-boilerplate
Boilerplate code for typescript development in Visual Studio Code

enter following commands:
- npm install tsd -g --save
- npm install
- tsd install --save

you should be able to run the app with F5
